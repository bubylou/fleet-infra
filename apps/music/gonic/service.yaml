---
apiVersion: v1
kind: Service
metadata:
  name: gonic
spec:
  selector:
    app: gonic
  sessionAffinity: ClientIP
  type: ClusterIP
  ports:
  - port: 4747
    targetPort: 80
    name: http
    protocol: TCP
