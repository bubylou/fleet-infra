---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-pvc
  labels:
    app: readarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-mirror-ssd
  resources:
    requests:
      storage: 5Gi
