---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-pvc
  labels:
    app: calibre
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
