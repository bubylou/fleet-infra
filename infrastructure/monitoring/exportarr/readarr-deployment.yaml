---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: readarr-exporter
  labels:
    app.kubernetes.io/name: readarr-exporter
    app.kubernetes.io/component: monitoring
    app.kubernetes.io/instance: readarr-exporter
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: readarr-exporter
      app.kubernetes.io/component: monitoring
      app.kubernetes.io/instance: readarr-exporter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: readarr-exporter
        app.kubernetes.io/component: monitoring
        app.kubernetes.io/instance: readarr-exporter
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "monitoring"
    spec:
      containers:
        - name: readarr-exporter
          image: ghcr.io/onedr0p/exportarr:v1.5.3
          imagePullPolicy: IfNotPresent
          args:
            - readarr
          env:
            - name: PORT
              value: "9707"
            - name: URL
              value: "http://readarr.arr.svc.cluster.local:8787"
            - name: APIKEY
              valueFrom:
                secretKeyRef:
                  name: exportarr
                  key: readarr-api-key
          ports:
            - name: monitoring
              containerPort: 9707
          livenessProbe:
            httpGet:
              path: /healthz
              port: monitoring
            failureThreshold: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: monitoring
            failureThreshold: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
